# Source Prompt

Use `$orchestrating-architecture-execution` to turn this initiative into a staged architecture-to-execution artifact pipeline.

Initiative:

Reduce failed checkouts for `checkout-api` while preserving release velocity.

Context:

- The customer outcome is paid order completion after cart submission.
- Current friction includes payment-provider failures, inventory timeouts, unclear ownership, weak release readiness signals, and incident learning that does not reach implementation work.
- The team has a broad product goal but no shaped capability, feature packet, C4 decision view, story packet, or implementation handoff.
- Keep the work human-sized; do not create a story factory.
- Use C4 only where architecture boundaries, ownership, integration, data, or deployment decisions affect delivery.
- Stop before coding, but prepare the packet so an implementation plan can be written next.

Produce these artifacts:

- `ValueStreamBrief`
- `CapabilityMap`
- `FeaturePackets`
- `C4DecisionViews`
- `StoryPacket`
- `TraceabilityReview`
- `ImplementationHandoff`
